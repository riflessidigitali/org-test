/**
 * @fileoverview Caseproof eslint plugin
 * @author Caseproof
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const requireIndex = require("requireindex");

//------------------------------------------------------------------------------
// Plugin Definition
//------------------------------------------------------------------------------
module.exports = {
    meta: {
        "name": "@caseproof/eslint-plugin",
        "version": "0.0.1"
    }
}

// Import all configs/rules.
const configItems = [
    'configs',
]
configItems.forEach((item) => module.exports[item] = requireIndex(__dirname + '/' + item));
